version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PYTHONUNBUFFERED=1
    # Uncomment the volumes line for live code reload during development
    # volumes:
    #   - .:/app
